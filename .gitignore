# dependencies
/node_modules
/.pnp
.pnp.*

# next
/.next/
/.next-build/
/out/
/build
next-env.d.ts

# production
.vercel

# optional standalone MkDocs build (`npm run docs:mkdocs`); the site does
# not use it — /docs is rendered from corevalley-docs/docs/*.md by Next.js
/corevalley-docs/site/

# typescript
*.tsbuildinfo

# env
.env
.env*.local

# misc
.DS_Store
Thumbs.db
*.pem
npm-debug.log*
.eslintcache

# Reference material is intentionally tracked, NOT ignored:
#   design_system/  (Claude skill + token source of truth)
#   reference/      (archived static site)
