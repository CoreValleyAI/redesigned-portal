Square icon-only control — toolbars, nav, row actions. Always pass `title` for a11y.

```jsx
<IconButton icon={<Icon name="settings" />} title="Settings" />
<IconButton icon={<Icon name="plus" />} variant="primary" title="New instance" />
```

Variants: `ghost` (default), `surface`, `primary` (Hydro).
