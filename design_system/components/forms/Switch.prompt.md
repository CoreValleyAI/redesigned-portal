Mechanical on/off toggle — settings, feature flags. On = Hydro track with glow.

```jsx
<Switch defaultChecked onChange={setAuto} />
<Switch checked={spot} onChange={setSpot} size="sm" />
```

Controlled via `checked` or uncontrolled via `defaultChecked`.
