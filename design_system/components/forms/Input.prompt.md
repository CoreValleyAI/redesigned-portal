Carbon text field — hairline border lights Hydro on focus. Use `mono` + a `$`/`>` prefix for CLI-flavored entry.

```jsx
<Input placeholder="search instances" prefix={<Icon name="search" size={15} />} />
<Input mono prefix="$" placeholder="corevalley deploy …" />
```

Sizes `sm | md | lg`. Pair with a `.cv-label` above for forms.
