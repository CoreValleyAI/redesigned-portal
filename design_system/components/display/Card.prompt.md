Surface container — raised Carbon with a hairline border. The default wrapper for grouped content.

```jsx
<Card>…</Card>
<Card accent interactive onClick={open}>Selected region</Card>
```

`accent` adds a Hydro left edge (highlighted/active state); `interactive` adds hover lift + pointer. Compose StatBlock, Badge, Button etc. inside.
