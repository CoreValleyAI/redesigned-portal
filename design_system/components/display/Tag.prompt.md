Mono filter / flag / region chip — selectable or removable. Lighter weight than Badge.

```jsx
<Tag selected onClick={toggle}>--gpu h200</Tag>
<Tag onRemove={() => drop(id)}>np-ktm-1</Tag>
```

Use `selected` for active filters (Hydro outline), `onRemove` to render a × affordance.
