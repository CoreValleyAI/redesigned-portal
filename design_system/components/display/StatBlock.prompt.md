Hero metric figure + mono label — the brand's data-forward device for stat clusters.

```jsx
<StatBlock value="~$0.04" label="per kWh hydro power" />
<StatBlock value="~40ms" label="latency to Mumbai" accent size="lg" />
```

Use exactly one `accent` (Hydro) figure per cluster. Keep numbers concrete and unit-tagged per brand voice.
